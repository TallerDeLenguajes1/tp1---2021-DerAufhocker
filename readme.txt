Los puntos 2 y 3 son los mismos porque ambos muestran la direccion de memoria de la variable "name"
en el punto 2 mostramos el contenido de una variable puntero que contiene la direccion de memoria de la variable "name"
y en el punto 3 mostramos la misma direccion a traves de el operador indireccion &, que sirve para obtener la direccion de memoria de una variable

En el punto 4 lo que hacemos es mostrar la direccion de meoria de una variable de tipo puntero.
que es un variable que contiene otra direccion de memoria(la de "name"), es por ese que ambas direcciones son distintas
